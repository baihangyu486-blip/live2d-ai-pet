# Changelog

## Unreleased

### Changed

- 产品显示名统一为「AI 伴侣 · AI Companion」，项目名保留 `live2d-ai-pet`；角色名称来自配置。
- 主界面改为「陪伴、回忆、角色」三个入口，聊天直接呈现在陪伴页，心情与关系归入可收起的陪伴状态。
- 视觉统一为奶白、樱花粉、爱心与圆润气泡；移除旧首页的重复摘要、独立心情页与昼夜房间装饰。
- 「回忆」页内切换记忆和日记时光，继续使用实际保存的记录、置顶状态与关系里程碑。
- 设置按连接、角色、陪伴、记忆、聊天、声音、应用七类整理，增加全局保存入口与键盘分类切换，保留原有控件和保存能力。
- Live2D 改为可选：无模型时进入正常陪伴界面，不自动回退到本地付费模型；导入、切换和停用不改写人设或清空记录。
- 模型操作按实际资源生成可用表情与动作，避免向任意导入模型展示不兼容的固定动作。
- `.gitignore` 与应用打包文件规则明确排除本地 `models/`；已有文件保留在磁盘。

### Preserved

- 完整聊天会话、图片与识屏附件、贴纸、语音输入、停止和重试。
- 消息引用、复制、收藏、撤回、语音文字、图片预览与表情库。
- 人设方案及还原、生成参数、主动陪伴、感知、记忆、世界书、羁绊和纪念日。
- 聊天记录管理与导出、TTS/STT、自启动和缓存管理。

### Fixed

- 配置导出递归清空 `apiKey`、移除 `apiKeyEncrypted`，包括嵌套服务配置中的同名字段。
- 不使用 Live2D 时保留聊天、记忆、日记与关系功能的入口，不以模型加载作为进入应用的条件。

### Release status

软件版本仍为 1.3.0，本节记录尚未公开发行的改动。浏览器隔离预览和模拟检查不等于真实 Electron 桌面验收；干净依赖安装、新打包与安装程序回归仍待完成。旧安装程序不代表当前源码。
