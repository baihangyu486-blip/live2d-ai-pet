# AI 伴侣 · 公开发行检查清单

项目作者：FuFu  
更新日期：2026-09-10  
状态：界面重构开发版，尚未据此发布新版本。

软件包版本仍为 1.3.0。浏览器预览、模拟测试和既有安装程序均不能替代当前源码的真实桌面与打包验收。

## 当前已落实的源码范围

- [x] 主界面整理为陪伴、回忆、角色，产品名与角色名分开。
- [x] 陪伴页复用完整聊天；设置分成七类，原有 130 个控件与容器 ID 保留。
- [x] Live2D 支持无模型、导入、已导入模型切换和停用；不以模型作为核心功能前提。
- [x] Git 忽略规则和 builder 文件规则均明确排除本地 `models/`，不删除本地资产。
- [x] 配置导出递归清空 `apiKey`，移除 `apiKeyEncrypted`。
- [x] README 与产品说明反映当前页面，不再宣传固定角色品牌或已移除的房间页。

这些勾选表示对应源码或规则存在，不代表实际打包产物已经检查完毕。

## 依赖与构建

- [ ] 同步 `package.json` 与 `pnpm-lock.yaml` 中的依赖声明，核对 `ws`。
- [ ] 统一 Electron 依赖与打包版本，去除对特定本机 `.store` 路径的依赖。
- [ ] 核实可访问的 Electron、builder、Pixi 和 rcedit 运行环境，处理此前的 EPERM 与启动退出问题。
- [ ] 在全新目录安装依赖，执行冻结锁文件安装并记录工具版本。
- [ ] 执行 `npm test`，记录当次实际结果；模拟检查与真实桌面结果分别记录。
- [ ] 执行 `npm run start`，验证真实 Electron 窗口启动。
- [ ] 执行 `npm run pack` 和 `npm run dist`，生成本轮源码的新产物。
- [ ] 检查实际应用包没有付费模型、真实配置、密钥、缓存和私人记录。
- [ ] 记录版本、构建时间、系统版本和安装包校验值。

当前工作只修改产品元数据与模型排除规则，没有把依赖整理或打包验收写成已完成。

## 无模型与核心陪伴

- [ ] 全新用户数据且不导入模型时，正常进入主界面、打开聊天与设置。
- [ ] 配置真实服务后，文字聊天、语音、附件与主动陪伴按既有设置工作。
- [ ] 心情、好感、打卡、记忆和日记读取实际状态，关闭形象不清空数据。
- [ ] 记忆置顶及主动陪伴暂停可以持久化，重复打卡不重复累计。
- [ ] 回忆中的日记、片段和里程碑保持时间顺序，空记录显示自然空状态。
- [ ] 服务或快照失败保留已显示内容并给出可理解的反馈。

## 完整聊天与设置

- [ ] 会话切换、新建、清空、重命名、置顶和删除回归。
- [ ] 图片、识屏附件、贴纸、语音输入、停止与重试回归。
- [ ] 引用、复制、收藏、撤回、语音文字、图片预览、表情库和贴纸编辑回归。
- [ ] 历史搜索、JSON/Markdown/完整备份导出回归。
- [ ] 七类设置均可到达，切换页面不丢未保存输入，全局保存覆盖当前配置。
- [ ] 人设方案、还原和另存，生成参数、世界书、主动行为和感知配置回归。
- [ ] TTS/STT、自启动、缓存、纪念日和提醒保存回归。
- [ ] 小工作区、125% 缩放、键盘导航、减少动态效果和多显示器回归。

## 可选 Live2D

- [ ] 导入有效 `.model3.json` 和关联资源后正确展示，原始文件保持完整。
- [ ] 缺文件、非法相对路径、无效清单、超量资源均被拒绝并有明确反馈。
- [ ] 切换不同模型只展示兼容动作，不使用缺失的固定角色资源。
- [ ] 停用后释放预览，保留导入目录、聊天、人设与记忆；可重新选择启用。
- [ ] 主界面角色预览和独立桌宠的显示、隐藏、最小化与恢复正常。
- [ ] 不在聊天页或回忆页持续运行不必要的主界面模型渲染。

## 数据、许可与公开素材

- [ ] 首次使用清楚说明活动与屏幕感知的状态、发送范围和关闭方式。
- [ ] 关闭感知后停止新增采集，检查真实 AI 请求的上下文范围。
- [ ] 完成 TTS 与其他凭据的保存保护，核实安全存储不可用时的行为。
- [ ] 配置导出和聊天备份分别核对私人数据范围。
- [ ] 确定源码许可证后补齐 `LICENSE`；没有确定前不宣称全部源码已有开放许可。
- [ ] 核对其余贴纸、图标和运行时的分发依据；保留第三方版权与许可声明。
- [ ] README、应用 About、包元数据和发行记录中的项目作者仅为 **FuFu**。
- [ ] 截图来自当前实现，示例数据有标识，未包含私人消息、密钥或用户模型素材。
- [ ] README 两张新截图存在，旧截图不再用于展示当前版本。
- [ ] 录制实际 Demo，公开功能与当前实现一致。

`.gitignore` 不能删除既有 Git 历史中的文件，也不替代实际打包内容检查。第三方许可声明与本项目作者栏分开维护。

## 实际发行

- [ ] 对本轮新安装包完成安装、首次启动、配置、退出重启和卸载检查。
- [ ] 更新 CHANGELOG 和正式版本号，提供实际产物与校验值。
- [ ] 在发行说明中记录已测试系统和已知限制。
- [ ] 完成上述验收并得到发布操作授权后，再创建公开 GitHub 发行。

此前真实 Electron 启动曾以状态 1 退出且无诊断输出；旧安装包不属于本轮源码构建。因此实际桌面与安装包验收仍保持待完成。
